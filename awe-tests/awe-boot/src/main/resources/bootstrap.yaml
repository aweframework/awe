# Spring Cloud Kubernetes disabled
spring:
  application:
    name: awe-boot
  cloud:
    kubernetes:
      enabled: false
    gcp:
      secretmanager:
        enabled: false
      core:
        enabled: false
# Logging
logging:
  file:
    path: /logs